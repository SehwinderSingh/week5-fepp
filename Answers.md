- When the backend sends a command as JSON, this code compies MongoDB's '\_id' into a new field called 'id'. It converts mongoose into json.

- CORS allows the frontend to connect and use backend resources. It is known as Cross Origin Resource Sharing

- Change origin helps make the frontend request look like the backend one. It helps shorten urls, no need for CORS and easy to deploy.
